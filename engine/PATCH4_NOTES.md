# ED-DI-001 Final Patch（顧客向けUnknown表示標準「分からない」への正式統一）実施報告

`ED_DI_001_FINAL_PATCH_INSTRUCTION.md`に基づき、`Energy_Doctor_Design_Issue_Log.md`の
ED-DI-001 Final Disposition（S社 Design Disposition、2026-09-02。顧客向けUnknown表示標準は
「分からない」で確定）に対応した。本Patchの内容は、**(1)** V2.3正本内で顧客向け選択肢として
「不明」が残っている箇所の特定（改訂自体はS社側作業）、**(2)** Forms Implementation Specが
既にFinal Dispositionと整合していることの確認、**(3)** コード側のInterim期の記述をFinal
Disposition表記へ更新、の3点。**Adapterの受理ロジック（「不明」「分からない」「空欄」→
UNKNOWN）、Web_EDI/DRI/EPIの算定式、TOP5順位ロジック、ED-DI-002/003/004/005で確定した仕様、
ISS-04/07/08は一切変更していない。** 正本ファイル（V2.2/V2.3のxlsx、Forms実装仕様）自体も
直接編集していない。

まず、指示どおりリポジトリ内`05_Handoff_Brief/Energy_Doctor_Design_Issue_Log.md`を今回の
Package同梱版で上書き済み（ED-DI-001＝Final Disposition Approved / Implementation
Pending、ED-DI-002/004/005＝Implementation Complete、ED-DI-003＝CLOSEDを反映）。

## 1. V2.3正本：(A) 修正候補一覧（顧客向け選択肢としての「不明」）

対象ファイル：`01_Core_Design/Energy_Doctor_LP_SelfDiagnosis_Design_V2_3_Traceability_Approved.xlsx`
（80シート）。ファイル自体は編集していない（改訂はS社側作業）。

| # | シート | セル | 現在の文言 | 備考 |
|---|---|---|---|---|
| 1 | `68_公開フォーム最小質問セット` | E6:E21（16セル、WQ-101〜WQ-405の「選択肢」列） | 各選択肢リストの末尾が「／不明」（例：E6=`把握している／一部把握／ほとんど把握していない／不明`） | 事前にS社が示した「主な修正候補」どおり。公開18問の回答選択肢そのもの。 |
| 2 | `58_公開フォーム項目` | G13, G14, G15, G16 | `不明`（FORM-009〜012、設備／エネルギー／建屋・環境／管理・運用の各質問群の「初期値」列） | 質問群の初期値として顧客画面に現れうる値。 |
| 3 | `58_公開フォーム項目` | L13 | `各質問に回答してください。不明も選択できます。`（FORM-009の「エラー文」列） | 未回答時に顧客へ表示されるエラー文言。選択肢名を直接指している。 |
| 4 | `76_MicrosoftForms実装仕様` | D6 | `10～15分、分からない項目は不明、ファイル添付なし`（MSF-002「仮仕様」列＝フォーム説明文の草案） | Microsoft Formsの説明欄に表示される文言の草案。「分からない項目は不明」という言い回し自体を「分からない項目は「分からない」を選択できます」等へ改める必要がある。 |
| 5 | `59_Web・メール文面` | D6 | `営業担当からご案内を受けた方は、分かる範囲でご回答ください。不明な項目は「不明」を選択できます。図面・写真等の資料添付はありません。`（MSG-002本文案） | 受付開始告知の文面案。引用符付きの「不明」＝選択肢名を直接指す。 |
| 6 | `65_LPワイヤーフレーム` | A20 | `...※分からない項目は「不明」を選択できます。`（PC版ワイヤーフレーム） | LPワイヤーフレーム内の注記文言。 |
| 7 | `66_LP掲載文章完成稿` | E18 | `分からない項目は「不明」を選択できます。情報が不足している場合は、その不足自体を診断結果に反映します。`（COPY-014完成稿） | LP掲載用の確定文言案。 |
| 8 | `66_LP掲載文章完成稿` | E34 | `初期版では10～15分程度を目安とします。分からない項目は「不明」を選択できます。`（COPY-030完成稿） | 同上、FAQ用。 |
| 9 | `57_LPページ仕様` | F8 | `設備、エネルギー、建屋・環境、管理・運用について、分かる範囲で回答します。不明を選択できます。`（LP-004「本文・掲載内容」） | LPページ本文案。 |
| 10 | `02_回答選択肢` | C20 | `点検未実施・不明`（ANS-FINDING4、選択肢4、内部値UNKNOWN） | 複合表示値の一部に「不明」が残存。他の大半の選択肢は既に「分からない」単独表記へ統一済みだが、この3件（#10〜#12）は「〜・不明」の複合表現のまま。 |
| 11 | `02_回答選択肢` | C24 | `対象・状況不明`（ANS-COMPLIANCE4、選択肢4、内部値UNKNOWN） | 同上。 |
| 12 | `02_回答選択肢` | C62 | `未管理・不明`（ANS-MAINT4、選択肢4、内部値UNKNOWN） | 同上。 |

**#10〜#12について：** Design Issue Logは「`02_回答選択肢`は既に『分からない』で整合済みの
見込み」としているが、実ファイルを走査した結果、上記3件（いずれも内部値`UNKNOWN`の選択肢）
は依然として「〜・不明」という複合表現のままであることが判明した。他の同種選択肢
（例：ANS-YESNOUNK、ANS-SEVERITY4等、19件）は表示値が単独の「分からない」へ既に統一されて
いるため、この3件は統一漏れの可能性がある。S社確認のうえ改訂要否をご判断ください。

## 2. V2.3正本：(B) 修正してはいけない箇所（内部処理・ルール名・試験名等）と判断理由

以下はいずれも「顧客が実際に選択する回答選択肢の表示文言」ではなく、内部処理・ルール名・
UI種別分類・試験名・過去経緯の記録等であるため、修正対象外と判断した。

| シート | セル（代表例） | 現在の文言 | 修正対象外と判断した理由 |
|---|---|---|---|
| `03_採点マトリクス` | H4（列見出し）、H5・H7・H8・H9等 | `不明時処理`／`不明＝スコア除外＋充足率減＋信頼度減`等 | 列名・内部ルール記述。指示書が明示的に例示した(B)区分そのもの。 |
| `03_採点マトリクス` | Q6（列見出し「DRI」下の値） | `不明` | 顧客選択肢とは無関係の内部分類列（値はEPI/L0/詳細/不明等が混在）。UnknownのUNKNOWN選択肢を指すものではない。 |
| `01_LP質問票` | F21 | `年＋不明` | 「回答形式」列＝入力方式の内部カテゴリ名（年入力＋Unknown選択肢の複合UI種別を指す分類ラベル）。実際の選択肢表示値は`02_回答選択肢`のANS-YEARKNOWN側（既に「分からない」）で定義される。 |
| `01_LP質問票` | I22 | `Q201が不明でも表示` | 「分岐条件」列＝内部分岐ロジックの説明文。 |
| `01_LP質問票` | E14 | `不明の場合は「分からない」` | 既にFinal Disposition相当の表記（「分からない」）を指しており、対象外どころか整合済みの参考例。 |
| `21_UI項目仕様` | E15 | `年入力＋不明` | `01_LP質問票`F21と同種のUI種別分類ラベル。 |
| `04_Engine_Mapping`相当（V2.3内の対応記述） | 各種 | `不明は0点にしない`等 | エンジンマッピングの内部ルール注記。指示書の事前確認事項と同一パターン。 |
| `05_Evidence判定` | E5、F6、F8、F9 | `対象不明確`／`版不明`／`型式不明`／`更新日不明` | Evidence（根拠資料）の内部判定ルール記述。顧客選択肢ではない。 |
| `07_コメントコード` | 多数（E7、E11、E21、E27、E29、E31、E35、E38、E41、E44、E48等） | `保守状況不明`／`異常有無不明`等（「発動条件概要」列）、`故障時の復旧条件が不明確な状態です。`等（「リスク解釈文」列） | 生成レポートのコメント発動条件・解釈文の内部ロジック記述。実際にレポートへ出力される文面ではあるが、これは「Unknown回答自体の選択肢表示」ではなく、Unknownを検知した結果として生成される診断コメント（ED-DI-005 review_itemsに相当する概念のV2.3側記述）であり、ED-DI-001（選択肢表示文言）の対象外。 |
| `08_レポート可変項目定義` | P7 | `不明回答を状態悪化と混同しない` | レポート生成方針の内部注記。 |
| `09_断定レベル・文章統制` | C7 | `不明回答、型式不明、資料なし、対象範囲不足` | 断定レベル判定の内部カテゴリ名。 |
| `11_テストケース` | C6、C9 | `Q106=YES、型式不明、メーカー回答なし`等 | テストケースの入力条件記述。指示書のFT-02（試験名扱い）と同種。 |
| `13_算定式・順位ロジック` | F5、D34 | `不明回答`／`影響大・代替なし・復旧不明ほど高得点` | 算定式・順位ロジックの内部ルール記述。 |
| `14_診断品質・文章生成` | D7、D8、D9、G25 | `完全一致=100...不明=20...`等、`不明・L0は有効回答に含めない` | スコアリングルールの内部記述。 |
| `20_LP画面遷移` | K9、D13 | `不明回答を許容`／`回答サマリー、不明項目、添付一覧、矛盾候補` | 画面遷移設計・内部確認画面の内容記述（後者は社内レビュー画面の項目リストであり顧客公開画面ではない）。 |
| `22_システム処理フロー` | H11、H12 | `資料種別不明`／`対象数不明` | システム処理フローの内部注記。 |
| `24_受入テスト` | B6 | `不明回答` | 受入試験の試験名。指示書事前確認のFT-02と同一パターン。 |
| `38_Baseline_Freeze` | F25 | `運営主体が不明な独立サイトとして公開` | Unknown回答とは無関係の一般用語（サイト運営主体の説明）。ED-DI-001の対象外。 |
| `39_開発バックログ` | J6 | `全必須質問・不明選択可` | 開発バックログ項目の内部記述。 |
| `40_要件トレーサビリティ` | D12 | `途中保存・進捗・不明回答` | 要件分類の内部記述。 |
| `52_Stage1実装仕様` | E6 | `必須・不明・選択値を検証` | 実装要件の内部記述。 |
| `53_Stage1データ項目` | C15 | `不明フラグ` | データ項目名（内部フラグ列名）。 |
| `55_Stage1受入テスト`、`61_Stage1A_Web受入テスト` | C6、B10、C10、D10等 | `全質問に回答し、不明を選択できる`等 | 受入試験の入力条件・試験名。指示書事前確認のFT-02と同一パターン（実際の顧客向け選択肢文言ではなく試験条件の記述）。 |
| `57_LPページ仕様` | K8、F13 | `不明回答を認め、情報不足を明示`／`...不明回答について回答します。` | 「顧客への約束」欄（内部方針の要約）およびFAQの話題名としての言及であり、選択肢の実際の表示文言（quoted「不明」）を直接指定するものではない。実際の表示文言は同シートF8・66シート等で別途A区分として捕捉済み。 |
| `65_LPワイヤーフレーム` | A44 | `...不明回答／AI利用...`（FAQ見出し一覧） | FAQ話題名の一覧。上記と同じ理由。 |
| `66_LP掲載文章完成稿` | J18（「注記・免責」列） | `不明を不利な点数として一律扱わない` | 編集方針・免責事項の内部注記であり、完成稿本文（E18、A区分で別途捕捉）とは別列。 |
| `67_LP実装注記` | D10 | `ファイル添付なし。不明選択可。同意必須。` | 実装要件の内部注記。 |
| `69_LP掲載A3サンプル仕様` | E6 | `架空回答17問＋一部不明` | サンプルテストデータの説明。 |
| `77_WQ-Q_Traceability`、`79_WQ-Q_Review` | H16、G20等 | `UNKNOWNは要確認事項。安全・品質影響が不明ならGuardrail判定保留候補。`等 | ED-DI-002/005関連の内部トレーサビリティ・ルール記述（ED-DI-002/003/004/005には触れない、という今回の変更禁止範囲に該当する内容でもある）。 |
| `68_公開フォーム最小質問セット` | A24（「Version2.3 Governance Note」） | `ED-DI-001 Interimにより公開FormsのUnknown表示は当面『不明』。Adapterは『不明』『分からない』『空欄』をUNKNOWNへ正規化。...` | 内部ガバナンス注記（過去経緯の記録）。顧客向け選択肢そのものではないため(B)に区分するが、**ED-DI-001がFinal Dispositionへ進んだ今、この注記の内容（「当面『不明』」）自体が古くなっている**。S社改訂時に本注記もFinal Disposition内容へ更新することを推奨事項として申し送る。 |

（`02_回答選択肢`のC95「小さい・不明確」は内部値`LOW`＝ANS-BENEFIT4の通常回答選択肢であり、
UnknownのUNKNOWN選択肢とは無関係のため、(A)/(B)いずれの対象でもない。）

## 2. Forms Implementation Specの整合確認結果

対象ファイル：`03_Microsoft_Forms/Energy_Doctor_Microsoft_Forms_Implementation_Spec_v1.0.xlsx`
（`01_Form_Settings`〜`06_GoLive_Checklist`、全6シート）を実際に開き、全セルを走査した。

**結論：S社の事前確認事項と完全に一致した。相違点なし。**

| 確認項目 | 実ファイルでの確認結果 |
|---|---|
| `01_Form_Settings` | A12=`「分からない」`、B12=`原則用意。不明回答は悪評価せず情報不足として扱う`。見出しは既に「分からない」、備考欄の「不明回答」は内部ルール説明（B区分相当）。 |
| `02_Questions` | WQ-101〜WQ-405に対応するF5〜F20の全16行で、選択肢リストの末尾がすべて`／分からない`（「不明」は1件も残存していない）。WQ-202/203/204/302/402/403を含め全問確認済み。 |
| `04_Engine_Mapping` | H4〜H21の全18行が`不明は0点にしない`という内部エンジンマッピングの備考。顧客向け選択肢ではない。 |
| `05_Acceptance_Test` | FT-02＝試験名`不明回答`、入力条件`複数設問で「分からない」`、期待結果`送信可。不明はUNKNOWN/空としてEngineで0点扱いしない`。試験名・期待結果欄の「不明」は内部記述、入力条件は既に「分からない」。 |
| その他（`03_Branching`、`06_GoLive_Checklist`） | 「不明」「分からない」いずれの文言も存在せず。 |

以上により、**このファイルはFinal Dispositionと完全に整合しており、改訂不要**であることを
実ファイル走査で確認した。S社の認識との相違点は見つからなかった。

## 3. コード側：Interim関連コメントの更新箇所一覧

| ファイル | 変更内容 |
|---|---|
| `engine/energy_doctor_engine/forms_adapter.py` | モジュールdocstringの「S社's decision for Corrective Patch 1 (Rev0.4, ISS-02): the public Forms display wording follows 68 (不明)...Which display text S社 ultimately standardizes on is ED-DI-001, and is not decided here」という**Interim期の記述**を、「**ED-DI-001 Final Disposition (S社 Design Disposition, 2026-09-02): the customer-facing Unknown display standard is now officially「分からない」**」という確定表記へ更新。V2.2/V2.3側の改訂対象箇所の特定は本ファイル（`PATCH4_NOTES.md`）を参照する旨も追記。**Adapterの受理ロジック（`_UNKNOWN_ALIASES = {"不明", "分からない", "わからない"}`、`normalize_forms_response()`）自体は一切変更していない。** |
| `engine/ISSUES.md` | 状況サマリ表（14行目以降の表）のED-DI-001行を、`OPEN / Interim Operational Disposition Applied`から`Final Disposition Approved / Implementation Pending`へ更新。ISS-02節の暫定運用の記録（Rev0.4時点の歴史的記述）はそのまま残し、その直後に「追記（2026-09-02、ED-DI-001 Final Patch）」として、顧客向け表示標準が「分からない」で確定した旨を追記（過去の記録自体は書き換えていない）。 |

**意味上残すべき「不明」（(A)/(B)の区別と同じ考え方）：** `web_kpi.py`のGuardrail urgency判定
（`elif c18 in ("不明", "", "UNKNOWN")`）、`wq_normalize.py`の`UNKNOWN_VALUES = {"不明", "",
"UNKNOWN"}`は、いずれも**Adapterをバイパスして直接呼び出された場合や、正規化前の生の
Forms_Response値を受け取った場合でも同じ結果になるようにする、既存の受理ロジックの一部**
であり、「顧客向け表示標準の記述」ではないため変更していない（変更禁止の「Adapterの受理
ロジック」に含まれる）。`task2/TASK2_REPORT.md`・`engine/PATCH1_NOTES.md`のInterim期に関する
記述は、いずれもその時点の状態を正しく記録した歴史的record（過去経緯説明としての「不明」、
指示書(B)区分の例示と同種）であるため書き換えていない。

## 4. 既存への影響がないことの確認

```
cd engine
python3 -m pytest -q
```

**73件全PASS**（Task 1A 19件＋Corrective Patch 1/1.1 17件＋Engine Patch 2 17件＝既存53件、
＋WQ Sufficiency Validation 18件＋ED-DI-003 Final Pipeline Patch純増2件＝既存73件、今回の
Patchによるテスト追加・変更なし）。

Task2の5シナリオ（`task2/run_scenarios.py`）を再実行し、`task2_results.json`に差分がない
ことを`git diff`で確認した（0バイト差分）。正本ファイル（V2.2/V2.3のxlsx、Forms実装仕様）は
`SHA256SUMS.txt`記載14ファイルすべてハッシュ一致を再確認済み（本Patchでは直接編集していない
ため当然の結果だが、機械的に再確認した）。

## 5. 変更したコード・ドキュメントの一覧

| ファイル | 変更種別 |
|---|---|
| `05_Handoff_Brief/Energy_Doctor_Design_Issue_Log.md` | 上書き（今回のPackage同梱版を正として反映） |
| `05_Handoff_Brief/ED_DI_001_FINAL_PATCH_INSTRUCTION.md` | 新規追加（参照用、無改変） |
| `engine/energy_doctor_engine/forms_adapter.py` | 変更（docstringのみ。受理ロジックは無変更） |
| `engine/ISSUES.md` | 変更（ED-DI-001サマリ行の更新、ISS-02節への追記） |
| `engine/PATCH4_NOTES.md`（本ファイル） | 新規追加 |

正本ファイル（`01_Core_Design/*.xlsx`、`03_Microsoft_Forms/*.xlsx`）は直接編集していない。
テストファイルの追加・変更もなし（既存73件がそのままPASSすることを確認したのみ）。

## 6. 残る作業（S社側）

- 本レポート「1. (A) 修正候補一覧」の12件（うち9件はLP/Forms系の顧客向け文言草案、3件は
  `02_回答選択肢`の複合表示値）を、V2.2/V2.3正本上でS社が改訂する。
- `68_公開フォーム最小質問セット`A24のガバナンス注記を、Interim表記からFinal Disposition
  表記へ更新する（(B)区分だが古い記述である旨を上記1節末尾で申し送り済み）。
- 改訂完了後、ED-DI-001のClose条件2・4・5（V2.2/V2.3正本内表記統一、Adapter仕様明文化＝
  今回本レポートで対応済み、回帰試験PASS＝今回73件で確認済み）を満たし、Close条件3
  （Forms Implementation Spec整合確認）は今回のPatchで完了。全条件充足後、S社にて
  ED-DI-001を完全Closeとする。
