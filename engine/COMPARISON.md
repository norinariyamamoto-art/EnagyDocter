# TC-A / TC-B / TC-C 比較表（Excel期待値 vs コード実行結果）

`pytest`（`engine/tests/`）で自動検証済み。実行方法は `README.md` を参照。

## TC-A（製造工場・混在リスク型）

**入力の正本：** `Forms_Response!A4:V4`（Case_ID=TC-WEB-A、実データ）。
**期待値の正本：** `Web_KPI!B5:B7`、`Guardrail!A4:H6`、`TOP5_Final!C4:J18`
（すべてExcelの数式が実際に計算した値）。

| 項目 | Excel期待値 | コード結果 | 判定 |
|---|---|---|---|
| Web_EDI | 43（要改善） | 43（要改善） | ✅ 一致 |
| Web_DRI | 36（判断困難） | 36（判断困難） | ✅ 一致 |
| Web_EPI | 80（最優先） | 80（最優先） | ✅ 一致 |
| Guardrail発動 | BCP・供給継続 L2（Priority Score 565） | BCP・供給継続 L2（Priority Score 565） | ✅ 一致 |
| TOP5 1位 | GR-01（安全・法令・品質・供給継続の未解決事項確認）score 81.3 | 同左 | ✅ 一致 |
| TOP5 2位 | EQ-03（重要設備のEOL・復旧リスク確認）score 74.3 | 同左 | ✅ 一致 |
| TOP5 3位 | BL-03（建屋環境による品質・操業影響の優先確認）score 66.3 | 同左 | ✅ 一致 |
| TOP5 4位 | BL-02（建屋点検・修繕優先順位の整理）score 62.3 | 同左 | ✅ 一致 |
| TOP5 5位 | EN-01（電力使用量・デマンド管理の高度化）score 60.8 | 同左 | ✅ 一致 |
| BL-01の扱い | TOP-R02によりBL-03に統合され候補外 | 同左（eligible=False） | ✅ 一致 |
| Mock_Test_Cases!K3記載順序（GR→EOL・復旧→建屋影響→建屋点検→電力・デマンド） | — | 完全一致 | ✅ 一致 |

**判定：TC-A は全項目で Excel の実測値と完全一致（PASS）。**

---

## TC-B（研究所＋事務棟・管理良好型）

**入力の正本：** なし（`Mock_Test_Cases!A7`により、TC-B/CはForms実装後に実測予定の
「ロジックレビュー条件」であり、Excel内に回答行が存在しない）。
`tests/fixtures.py`の`TC_B_FORMS_RESPONSE`は、`Mock_Test_Cases!D4`の文章条件
（「設備情報・故障履歴・代替・電力管理・建屋点検・中期計画が概ね整備／重大未解決なし」）
を満たすよう、該当する全項目を最良回答（かつWQ-404=「ない」）にして構築した。

| 項目 | Mock_Test_Cases記載の目標値 | コード結果 | 判定 |
|---|---|---|---|
| Web_EDI | 100 | 100（良好） | ✅ 一致（※目標値自体がExcel実測ではなく記載値。ISS-01参照） |
| Web_DRI | 100 | 100（判断可能） | ✅ 一致（同上） |
| Web_EPI | 18 | 18（低優先） | ✅ 一致（同上） |
| Guardrail発動 | なし | なし | ✅ 一致 |
| TOP5件数 | 0件（無理に5件表示しない） | 0件（発火issueなし） | ✅ 一致 |

**判定：構築した入力データに対し、記載目標値と完全一致（PASS）。ただし目標値自体が
Excelの実測ではなく`Mock_Test_Cases`シートへの手動記載値であるため、「Excelの
ライブ計算結果との照合」ではなく「文章条件を満たす入力を作ったときにロジックが
記載値どおりに振る舞うことの確認」である点に注意（ISS-01参照）。**

---

## TC-C（物流センター・重大Guardrail型）

**入力の正本：** なし（TC-Bと同様、`Mock_Test_Cases!A7`によりExcel内に回答行なし）。
`Mock_Test_Cases!D5`（「法令または安全の未解決／代替なし／管理基準なし／中期計画
なし／3か月以内」）で固定される項目（WQ-103/402/403/404/405）以外は、目標値
（EDI27/DRI22/EPI91）に一致する組み合わせを、選択肢の離散空間に対する全探索
（約183,378通り）で1件特定して採用した（`tests/fixtures.py`参照）。

| 項目 | Mock_Test_Cases記載の目標値 | コード結果 | 判定 |
|---|---|---|---|
| Web_EDI | 27 | 27（要優先対応） | ✅ 一致（探索により発見。ISS-01参照） |
| Web_DRI | 22 | 22（判断保留） | ✅ 一致（同上） |
| Web_EPI | 91 | 91（最優先） | ✅ 一致（同上） |
| Guardrail発動 | 安全・法令 L2 | 安全・法令 L2（Priority Score 665） | ✅ 一致 |
| 同点下での優先順位 | 安全・法令Guardrailが同点条件下でも最上位 | GR-01とBL-03がスコア93.0で同点となり、TiePriority（Guardrail=1 < その他=2）によりGR-01が1位を維持 | ✅ 一致（実際に同点条件を再現できた） |
| TOP5 2位以降 | 「代替性、計画・管理課題」が上位 | BL-03(建屋影響)→BL-02(建屋点検)→EQ-03(代替手段/EOL統合)→MG-03(中期計画ロードマップ) | ✅ 方向性一致 |

**判定：構築した入力データに対し、記載目標値と完全一致し、かつBrief記載の
「安全・法令Guardrailが同点条件下でも最優先に来ること」という検証観点を
実際にタイ状態(93.0対93.0)を再現した上で確認できた（PASS）。ただし、TC-Bと同様
目標値自体がExcel実測ではなく記載値であり、かつ入力自体が探索によって
事後的に見つけた一例である点に注意（唯一の正解入力である保証はない。ISS-01参照）。**

---

## 総括

- **TC-A**：Excelが実際に計算した値との完全一致を確認済み（最も信頼度の高い回帰テスト）。
- **TC-B/TC-C**：Excel内に実測データが存在しないため、文章条件＋記載目標値を満たす
  入力を構築し、その入力に対してロジックが期待どおりに振る舞うことを確認した
  （Guardrail種別・発動有無、TOP5の構造的性質［0件表示禁止・同点タイブレーク］は
  入力の作り方に依存しない普遍的な検証ができている）。
- 3ケースとも`pytest`で自動実行・PASS（`engine/tests/test_tc_a.py` /
  `test_tc_b.py` / `test_tc_c.py`）。
