# ED-DI-001｜V2.3 正本改訂後 Verification 実施報告

`ED_DI_001_V2_3_VERIFICATION_INSTRUCTION.md`に基づき、S社が改訂したV2.3正本
（`Energy_Doctor_LP_SelfDiagnosis_Design_V2_3_Traceability_Approved.xlsx`）の改訂結果を
検証した。**本タスクはコード変更を一切伴わない検証作業。** Engineコード・Adapterロジック・
Web_EDI/DRI/EPI算定式・TOP5順位ロジック・ISS-04/07/08・ED-DI-002〜005確定仕様には
一切触れていない。

**唯一の例外的な操作として、検証がすべてPASSしたことを受け、リポジトリが保持する
V2.3正本のコピー（`01_Core_Design/Energy_Doctor_LP_SelfDiagnosis_Design_V2_3_Traceability_Approved.xlsx`）を
今回受領したAFTER版（S社改訂済み、内容は一切変更せずそのまま配置）に差し替え、
`SHA256SUMS.txt`のハッシュ値を更新した。** これはExcelの中身を自分で編集する行為
（禁止されている操作）ではなく、S社から届いた改訂済みファイルをリポジトリの追跡対象と
して置き換える操作であり、Design Issue Log・各種指示書等、これまでの全フェーズで
繰り返してきた「S社改訂版の正本をリポジトリへ反映する」という運用と同じ扱いである。
検証以外の目的でこのファイルの中身を書き換えた事実はない。差し替え前のBEFORE版の
SHA256（`7371dbecd0...`）は本レポートに記録し、監査証跡として残す。

## 0. 前提：Governance Noteの所在訂正

同梱の`SOURCE_LOCATION_CORRECTION.md`により、Governance Noteの実際の所在は
`76_MicrosoftForms実装仕様!A24`ではなく`68_公開フォーム最小質問セット!A24`であることが
S社側で確認・訂正された（これは筆者が前回のED-DI-001 Final Patchで実ファイル走査により
特定していた箇所と一致する）。したがって本検証の差分期待値は**30セル＋
`68_公開フォーム最小質問セット!A24`の計31セル**として扱った。`76_MicrosoftForms実装仕様!A24`
は今回のAFTER版でも空欄のままであることを確認済み（下記2節）。

## 1. 正本差分の確認（完了条件1）

同梱の2ファイルを直接比較した。

- BEFORE：`Energy_Doctor_LP_SelfDiagnosis_Design_V2_3_Traceability_Approved_BEFORE_ED_DI_001.xlsx`
  （SHA256: `7371dbecd005549a8f63c6e1607c5f514b5b3e413ffafe82c37f44dbd5b60fc0`）
- AFTER：`Energy_Doctor_LP_SelfDiagnosis_Design_V2_3_Traceability_Approved.xlsx`
  （SHA256: `d75b963eb33a5d0ceb65539b00351f9e9257ab75275c76ccf6421fe5ec0cb6e2`）

両ファイルのSHA256は`VERIFICATION_MANIFEST.json`記載の値と完全一致することを確認した。
また、リポジトリに従来から記録されていたV2.3ファイルのSHA256（`SHA256SUMS.txt`）が
BEFORE版のハッシュと一致することを確認し、**リポジトリが確かに改訂前の状態を保持していた
こと**を裏付けた。

openpyxlで全80シート・全セルを、値ベース（`data_only=True`）・数式ベース
（`data_only=False`）の両方で突合した。加えて、シート名・シート順序・定義済み名前
（defined names）・各シートの次元（dimensions）・結合セル範囲・セルのフォント・
数値書式についても差分の有無を機械的に確認した。

**結果：値・数式のいずれの比較でも、変更セルは正確に31件で、`ED-DI-001_V2.3_Revision_Table.md`
記載の30セル＋Governance Noteの1セルと完全に一致した。それ以外のセル・シート構造・
書式・定義済み名前には一切差分がなかった。** 想定外の差分は0件であり、Issue報告事項なし。

| # | シート | セル | 改訂前 | 改訂後 | 判定 |
|---:|---|---|---|---|---|
| 1〜16 | `68_公開フォーム最小質問セット` | E6:E21 | 各選択肢末尾「／不明」 | 各選択肢末尾「／分からない」 | 一致 |
| 17〜20 | `58_公開フォーム項目` | G13:G16 | `不明` | `分からない` | 一致 |
| 21 | `58_公開フォーム項目` | L13 | `各質問に回答してください。不明も選択できます。` | `各質問に回答してください。分からない場合は「分からない」を選択できます。` | 一致 |
| 22 | `76_MicrosoftForms実装仕様` | D6 | `10～15分、分からない項目は不明、ファイル添付なし` | `10～15分、分からない項目は「分からない」を選択、ファイル添付なし` | 一致 |
| 23 | `59_Web・メール文面` | D6 | `...不明な項目は「不明」を選択できます。...` | `...分からない項目は「分からない」を選択できます。...` | 一致 |
| 24 | `65_LPワイヤーフレーム` | A20 | `...※分からない項目は「不明」を選択できます。` | `...※分からない項目は「分からない」を選択できます。` | 一致 |
| 25 | `66_LP掲載文章完成稿` | E18 | `分からない項目は「不明」を選択できます。...` | `分からない項目は「分からない」を選択できます。...` | 一致 |
| 26 | `66_LP掲載文章完成稿` | E34 | `...分からない項目は「不明」を選択できます。` | `...分からない項目は「分からない」を選択できます。` | 一致 |
| 27 | `57_LPページ仕様` | F8 | `...不明を選択できます。` | `...分からない場合は「分からない」を選択できます。` | 一致 |
| 28 | `02_回答選択肢` | C20 | `点検未実施・不明` | `分からない` | 一致 |
| 29 | `02_回答選択肢` | C24 | `対象・状況不明` | `分からない` | 一致 |
| 30 | `02_回答選択肢` | C62 | `未管理・不明` | `分からない` | 一致 |
| 31 | `68_公開フォーム最小質問セット` | A24 | Interim期の記述 | Final Disposition確定内容 | 一致 |

## 2. 顧客向けUnknown表示の統一確認（完了条件2）

AFTER版を実ファイルで直接確認した。

- `68_公開フォーム最小質問セット` E6:E21（16セル）：全て末尾が「／分からない」で統一。
- `58_公開フォーム項目` G13:G16（初期値）・L13（エラー文）：全て「分からない」。
- `76_MicrosoftForms実装仕様` D6：「分からない項目は「分からない」を選択」に更新済み。
- `59_Web・メール文面` D6、`65_LPワイヤーフレーム` A20、`66_LP掲載文章完成稿` E18・E34、
  `57_LPページ仕様` F8：いずれも「分からない」で統一。
- `02_回答選択肢` C20・C24・C62：**単独の「分からない」**（複合表現「〜・不明」は解消済み）
  であることを実ファイルで確認した。内部値（D列）は3件とも`UNKNOWN`のまま変更なし、
  選択肢ID（A列）も`ANS-FINDING4`／`ANS-COMPLIANCE4`／`ANS-MAINT4`のまま変更なし。

**結論：顧客向けUnknown表示は例外なく「分からない」に統一されている。**

## 3. Governance Note確認（完了条件3）

`68_公開フォーム最小質問セット!A24`の実際の値：

> ED-DI-001 Final Disposition（2026-09-02）により、公開FormsのUnknown顧客向け表示標準は
> 「分からない」とする。内部標準値はUNKNOWNを維持し、「分からない」「不明」「空欄」は
> 後方互換のためUNKNOWNとして受理する。

`ED-DI-001_V2.3_Revision_Table.md`記載の「改訂後」文言と1文字違わず完全一致することを
確認した。

`76_MicrosoftForms実装仕様!A24`はAFTER版でも`None`（空欄）のままであり、**新規作成・
変更のいずれも行われていない**ことを確認した（同シートの最大行は24行のままで、行自体が
追加されてもいない）。

## 4. 内部表記・ロジック不変の確認（完了条件4）

| 確認対象 | 結果 |
|---|---|
| 内部値`UNKNOWN`（`02_回答選択肢`D列、全26行） | 全行で変更なし（差分0件） |
| 選択肢ID（`ANS-FINDING4`／`ANS-COMPLIANCE4`／`ANS-MAINT4`等、A列） | 全行で変更なし（差分0件） |
| `03_採点マトリクス`の「不明時処理」列（H列見出し・内容） | 見出し・内容とも一切変更なし（例：H5=`不明＝スコア除外＋充足率減＋信頼度減`のまま） |
| `13_算定式・順位ロジック`（V2.3側のGuardrail・TOP5・採点ロジック記述シート） | 差分0件 |
| `77_WQ-Q_Traceability`（ED-DI-002確定内容） | 差分0件 |
| その他V2.3全80シート（上記31セル以外） | 差分0件（シート数・シート順・結合セル・フォント・数値書式も含め完全一致） |

「04_Engine_Mapping」は`03_Microsoft_Forms/Energy_Doctor_Microsoft_Forms_Implementation_Spec_v1.0.xlsx`
側のシートであり、本パッケージのBEFORE/AFTER V2.3ファイルには含まれない。同ファイルは
今回のPackageに同梱されておらず、リポジトリの`SHA256SUMS.txt`と実ファイルのハッシュが
引き続き一致していることを確認した（＝今回のV2.3改訂によって触れられていないことを裏付け）。

Web_EDI/Web_DRI/Web_EPIの実際の算定式（`02_Diagnosis_Engine/...v1.4...xlsx`のWeb_KPIシート）
はそもそも本Packageの対象外であり、Engineコード（`web_kpi.py`等）も一切変更していない。

**結論：内部表記・列名・選択肢ID・Guardrail/TOP5/採点ロジックに関わる記述は一切変更されて
いない。**

## 5. 既存への影響がないことの確認（完了条件5・6）

```
cd engine
python3 -m pytest -q
```

**73件全PASS**（既存テストの期待値・件数に変更なし）。

`task2/run_scenarios.py`を再実行し、`task2_results.json`を`git diff`で確認した結果、
**差分ゼロ**（byte-for-byte一致）であることを確認した。Task2はEngineコード
（`02_Diagnosis_Engine`のExcel、Pythonモジュール）のみを参照し、V2.3の文言はEngineの
判定に一切使用されないため、この結果は当然だが、機械的に再確認した。

## 6. Issue報告（完了条件7）

**今回の検証で、Revision Table記載の31セル以外に想定外の差分は見つからなかった。
報告すべきIssueなし。**

参考として、`68_公開フォーム最小質問セット!A24`のGovernance Note文言自体は、
「後方互換のため」という表現でAdapterの3値受理ロジックの根拠を簡潔に説明しているが、
V2.3側のガバナンス注記であり、Engine側のドキュメント（`forms_adapter.py`のdocstring、
`PATCH4_NOTES.md`）と表現が完全に同一ではない（同義の内容を別の言葉で記述している）。
これは不整合ではなく単なる文体差と判断し、Issueとしては報告しない。

## 7. リポジトリへの反映内容

| ファイル | 変更内容 |
|---|---|
| `01_Core_Design/Energy_Doctor_LP_SelfDiagnosis_Design_V2_3_Traceability_Approved.xlsx` | S社改訂済みAFTER版に差し替え（Claude Codeによる直接編集は一切なし。BEFORE版SHA256は本レポート1節に記録） |
| `SHA256SUMS.txt` | 上記ファイルのハッシュ値を`7371dbecd0...`→`d75b963eb3...`へ更新 |
| `05_Handoff_Brief/ED-DI-001_V2.3_Revision_Table.md` | 新規追加（参照用、無改変） |
| `05_Handoff_Brief/ED_DI_001_V2_3_VERIFICATION_INSTRUCTION.md` | 新規追加（参照用、無改変） |
| `05_Handoff_Brief/SOURCE_LOCATION_CORRECTION.md` | 新規追加（参照用、無改変） |
| `engine/ED_DI_001_V2_3_VERIFICATION_REPORT.md`（本ファイル） | 新規追加 |

Engineコード（`energy_doctor_engine/`配下）・テストファイル・`task2/`のスクリプトや
出力データはいずれも変更していない。

## 8. まとめ

| # | 完了条件 | 結果 |
|---|---|---|
| 1 | workbook logical-cell diff = 別紙31セルのみ | **PASS**（値・数式両モードで31件、それ以外の差分は書式・構造含め0件） |
| 2 | 顧客向けUnknown = 「分からない」 | **PASS**（30セル全て確認、C20/C24/C62は単独表記） |
| 3 | Governance Note = `68_公開フォーム最小質問セット!A24` Final Disposition | **PASS**（文言完全一致、76!A24は空欄のまま） |
| 4 | 内部表記・ロジック不変 | **PASS**（UNKNOWN・選択肢ID・03採点マトリクス・13算定式ロジック等、差分0件） |
| 5 | 73 tests PASS | **PASS** |
| 6 | Task2 5シナリオ zero diff | **PASS**（`git diff`で確認） |
| 7 | 問題があればIssue報告 | 該当なし（想定外の差分・不整合は検出されなかった） |

ED-DI-001のClose条件2（V2.2/V2.3正本内の表記統一）はこれをもって完了と判断できる状態に
ある（最終のClose判定・ステータス更新はS社側の作業）。
